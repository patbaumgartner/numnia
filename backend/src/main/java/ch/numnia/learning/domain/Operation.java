package ch.numnia.learning.domain;

/** Basic arithmetic operations supported by the training mode (FR-LEARN-003). */
public enum Operation {
    ADDITION,
    SUBTRACTION,
    MULTIPLICATION,
    DIVISION
}
